#include <conio.h>
#include <stdio.h>
#include <math.h>
//nome(x) rua(x) numero(x) bairro(x) cep(x) cidade(x)
char option[1];
main (){
    fflush(stdin);

    printf("\ndeseja realizar outra operação 's' ou 'n'?");
    gets(option);
    printf("%s", option);

}