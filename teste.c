#include <conio.h>
#include <stdio.h>
#include <math.h>
//nome(x) rua(x) numero(x) bairro(x) cep(x) cidade(x)

float x;
float y;

main()
{
    x = 100;
    y = sqrt(x-37);
    printf("a raiz de x = %f", y);
}

